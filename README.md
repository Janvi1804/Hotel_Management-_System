A simple desktop-based Hotel Management System built using Python, Tkinter, and SQLite.
This application allows hotel staff to manage room bookings, update guest records, search bookings, delete/check-out guests, and export the booking data to CSV.

🚀 Features
✅ Booking Management

Add new guest bookings

Edit/update existing bookings

Delete bookings (Check-out)

Prevent duplicate room bookings (optional logic included)

✅ Search & Filter

Search by guest name or room number

Clear search to view all bookings again

✅ Table View

View all bookings in a sortable table

Auto-updating table after every action

✅ Export Data

Export all bookings to a CSV file (compatible with Excel)

✅ Persistent Storage

Uses SQLite database (hotel.db)

Automatically creates tables if not found
